# --- local dev (SQLite) ---
DATABASE_URL="file:./dev.db"
PORT=4000
CORS_ORIGIN="http://localhost:3000"
JWT_SECRET="change-me"
OTP_SECRET="change-me"
ADMIN_EMAILS="demo@skyroute.test"
MAIL_FROM="SkyRoute Travels <no-reply@skyroute.test>"

# --- production (set on Render) ---
# DATABASE_URL="postgresql://USER:PASS@HOST:5432/DB"   # run: npm run use:postgres first
# CORS_ORIGIN="https://your-app.vercel.app"

# --- optional: real email (else OTP prints to logs) ---
# SMTP_HOST="smtp.example.com"
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER="apikey"
# SMTP_PASS="secret"

# --- optional: real payments + live IRCTC fares ---
# STRIPE_SECRET_KEY="sk_test_..."
# IRCTC_API_KEY="..."
